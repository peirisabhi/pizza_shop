<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.colombopizza">

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the "MyLocation" functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ColomboPizza">

        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity android:name=".ui.main.ui.orderDetails.OrderDetailsActivity" />
        <activity android:name=".ui.main.ui.orders.OrdersActivity" />
        <activity android:name=".ui.main.ui.appetizers.ManageAppertizersActivity" />
        <activity android:name=".ui.main.ui.beverages.ManageBeveragesActivity" />
        <activity android:name=".customer.ui.checkout.CheckoutActivity" />
        <activity android:name=".customer.ui.cart.CartActivity" />
        <activity
            android:name=".customer.CustomerHomeActivity"
            android:label="@string/title_activity_customer_home" />
        <activity
            android:name=".ui.main.AdminHomeActivity"
            android:label="@string/title_activity_admin_home"
            android:theme="@style/Theme.ColomboPizza.NoActionBar"></activity>
        <activity android:name=".Menu3" />
        <activity android:name=".Menu2" />
        <activity android:name=".ManageItemsDetails3" />
        <activity android:name=".ui.main.ui.pizza.ManagePizzaItems" />
        <activity android:name=".three" />
        <activity android:name=".ViewOrder" />
        <activity
            android:name=".Category"
            android:label="@string/title_activity_category"
            android:theme="@style/Theme.ColomboPizza.NoActionBar" />
        <activity android:name=".productview" />
        <activity android:name=".ManageItems" />
        <activity android:name=".PizzaMenu" />
        <activity android:name=".AdminConfirm" />
        <activity android:name=".Successful_Payment" />
        <activity android:name=".AdminSignin" />
        <activity android:name=".Memberlogin" />
        <activity android:name=".Adminlogin" />
        <activity android:name=".ViewDetails" />
        <activity android:name=".ManageItemsDetails" />
        <activity android:name=".ManageMemberDetails" />
        <activity android:name=".ManageAdminDetails" />
        <activity android:name=".Administration" />
        <activity android:name=".MemberSignin" />
        <activity android:name=".Login" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>